global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090

  - job_name: mock-osmosis-target
    metrics_path: /metrics
    static_configs:
      - targets:
          - mock-osmosis-target:9191
