# About UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a premium About section below the transfer surface with an interactive side-view wormhole explainer, stage-by-stage hover/click inspection, and curated documentation cards.

**Architecture:** Extend the landing-page transfer state into a scrollable narrative layout, then render a dedicated `AboutSection` component under the transfer card. Keep the interaction self-contained in the new component so the existing transfer flow and progress panel remain unchanged.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, CSS

---

### Task 1: Landing-page behavior

**Files:**
- Modify: `web/src/app/App.test.tsx`
- Modify: `web/src/app/layout/LandingPage.tsx`

- [ ] Add a failing test that expects the transfer view to render a new About section below the card.
- [ ] Run `cd web && npm test -- src/app/App.test.tsx` and confirm the new assertion fails for the missing About content.
- [ ] Update the landing-page layout so the transfer state can render a stacked transfer card and About section.
- [ ] Re-run `cd web && npm test -- src/app/App.test.tsx` and confirm the landing test passes.

### Task 2: Interactive About section

**Files:**
- Create: `web/src/features/about/AboutSection.tsx`
- Create: `web/src/features/about/about-content.ts`
- Modify: `web/src/app/App.test.tsx`

- [ ] Add a failing test that expects the About section to show the default active stage and update the inspector content when another stage is selected.
- [ ] Run `cd web && npm test -- src/app/App.test.tsx` and confirm the new stage-interaction test fails before implementation.
- [ ] Build the About section with a side-view wormhole scene, small dark-blue stage markers, click/hover selection state, and curated docs data.
- [ ] Re-run `cd web && npm test -- src/app/App.test.tsx` and confirm the new interaction test passes.

### Task 3: Visual polish and motion

**Files:**
- Modify: `web/src/styles/global.css`

- [ ] Add a failing test only if a structural class or accessible label is needed for the new section; otherwise verify through build plus existing behavior tests.
- [ ] Implement the About-section visual system: space background treatment, wormhole tunnel, stage marker styling, inspector panel styling, doc-card layout, and reduced-motion-safe transitions.
- [ ] Run `cd web && npm test -- src/app/App.test.tsx src/features/bridge/bridge.test.tsx` and confirm nothing regresses.

### Task 4: Final verification

**Files:**
- Modify: `web/src/app/layout/LandingPage.tsx`
- Modify: `web/src/features/about/AboutSection.tsx`
- Modify: `web/src/styles/global.css`

- [ ] Run `cd web && npm run build`.
- [ ] Run `git diff --check`.
- [ ] Review the final layout quickly for copy alignment, mobile safety, and whether the About section still feels subordinate to the transfer flow.
